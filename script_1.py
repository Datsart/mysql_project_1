import pymysql

# con = pymysql.connect(
#     host='localhost',
#     user='root',
#     password='19980723',
#     database='test_1'
# )
# with con:
#     cur = con.cursor()
    # cur.execute("SHOW TABLES")
    # print(cur)
    # for i in cur:
    #     print(i)
    # cur.execute("SELECT * FROM categories")
    # rows = cur.fetchall()
    # for row in rows:
    #     print(row[5])
    # cur.execute("SELECT * FROM categories WHERE name = 'Торкрет бетоны'")
    # res = cur.fetchall()
    # for i in res:
    #     print(i)
    # cur.execute("DELETE FROM categories WHERE (`id` = '2')")
    # cur.execute(
    #     "INSERT INTO `test_1`.`categories` (`parent_id`, `group_id`, `lvl`, `section_id`, `name`, `text`, `alias`, `meta_title`, `meta_description`, `meta_keywords`, `status`, `position`) VALUES ('3', '0', '1', '1', 'Кроссовки', 'barkhatnye', 'shoes', 'sport', 'custom', 'natural skin', '1', '169')")
    # cur.execute("CREATE DATABASE new_database")


